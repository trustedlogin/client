; Disable .user.ini caching so Wordfence's WAF auto_prepend (set up
; per-spec in compat-wordfence.spec.ts beforeAll) takes effect on the
; next request instead of after the 5-min default cache.
user_ini.cache_ttl = 0
